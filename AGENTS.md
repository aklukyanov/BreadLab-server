# Rules

- NEVER install/run/start Django or any Python packages without explicit permission.
- NEVER run `pip`, `pip3`, `python3 -m pip`, or any package manager without explicit permission.
- NEVER modify files outside the project without explicit permission.
- NEVER open, read, or in any way touch `.env` files.
- Code analysis only through reading and grep — no runtime testing.
